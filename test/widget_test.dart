import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App launches', (WidgetTester tester) async {
    // Placeholder test — integration tests require a running backend
    expect(true, isTrue);
  });
}
